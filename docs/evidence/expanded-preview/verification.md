# 放大预览修复验证

- 原因：右上角展开图标原先只执行缩放恢复到 100%，默认状态下没有可见反馈。
- 修复：打开接近窗口大小的预览弹窗，共用当前 `Diagram`、布局和预览快照；支持 50%–200% 缩放、适配宽度、关闭按钮和 Esc 返回。主画布百分比按钮保留恢复 100% 的功能。
- Chrome 自动验证：流程图、甘特图，1440×900、1920×1080、390×900；覆盖空画布禁用、打开、实际尺寸增大、缩放、滚动边界、适配、关闭、Esc、焦点归还，以及数据/版本/dirty 不变。无页面运行错误。
- `scripts/verify-preview-zoom.mjs`：两图种三个视口的原有主画布缩放检查通过。
- `npm run quality`：35 项后端测试、95 项前端测试、契约检查、类型检查与构建通过。
- 截图：新增弹窗沿用现有配色、按钮和圆角。长图在弹窗内部滚动，关闭和缩放控件保持可见。工作台区域未重排。
- 使用仓库合成示例及本地布局，未调用外部模型；本次验证范围为预览交互。
