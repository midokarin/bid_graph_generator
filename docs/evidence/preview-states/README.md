# 初始空白与生成状态验证

- 初始画布为空，隐藏示例图、示例标记、缩放控件与空闲日志条。
- 新任务开始后显示居中的加载动画和“正在生成”；运行详情默认折叠，旧错误提示清除。成功后显示图表。重新生成时暂时隐藏旧图，取消或失败仍保留已有版本。
- Chrome 浏览器使用拦截的本地契约数据和 SSE 响应，未调用外部模型。流程图、甘特图均通过：首次空白、生成状态、首次失败后空白、成功展示、重新生成后取消恢复原图、版本数量检查。
- 1440×900 和 1920×1080 截图覆盖初始和生成状态；无页面脚本错误。
- `npm run quality` 通过：后端测试、契约检查、前端 85 项测试、类型检查和构建。
- `scripts/verify-preview-states.mjs` 可复验；`BIAOSHU_WEB_URL` 指定本地前端，`PLAYWRIGHT_MODULE` 指定 Playwright 模块。
