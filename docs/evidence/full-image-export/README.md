# 完整图片导出验证

- 移除纸型、纸张方向与额外纸张留白。保留图题和三种背景，旧纸型配置忽略。
- `npm run quality` 通过：后端、契约、前端 85 项测试、类型检查和构建。
- Chrome 无头浏览器使用本地契约示例，未调用外部模型。流程图与甘特图分别截图，视口 1440×900、1920×1080。
- 实际下载 PNG 并读取文件尺寸：流程图 1500×1500、甘特图 2700×1764（均不含图题）。白色、透明、自定义背景均检查角落像素，全部符合设置；无页面脚本错误。
- `scripts/verify-full-image-export.mjs` 可复验；通过 `BIAOSHU_WEB_URL` 指定本地前端，通过 `PLAYWRIGHT_MODULE` 指定 Playwright 模块。
