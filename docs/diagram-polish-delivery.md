# 流程图文字与版式优化（2026-09-11）

参考用户提供的完整中文流程图，调整正式 frontend 绘图与布局，不修改 demo。

- 中文节点按完整字符串逐行绘制，使用明确坐标和居中对齐，补充中文字体回退；流程图字号与行距增大，导出和预览共用组件。
- 纵向单主线及直接整改回路采用居中排版，整改节点与判断节点并排，条件标签与回路留出间距。其他拓扑及横向布局继续使用 ELK。
- ELK 节点宽高同步增加，长文字的判断节点扩大以容纳文本；可读性检查与实际字号、行距一致。
- 模型提示词明确要求完整业务短语与完整条件，不得截为首字。

## 验证

`npm run quality` 通过：契约生成一致性、后端测试、75 项前端测试、类型检查与生产构建。

在独立前端端口 5187 使用 agent-browser 验证。通过浏览器导入本地合成数据，调用正式布局、版本存储和 PNG 导出函数；未调用外部模型，不代表真实模型生成验收。

- [1920px 工作台](evidence/diagram-polish/workbench.png)
- [390px 页面](evidence/diagram-polish/mobile.png)：页面宽度 390，无全页横向溢出。
- [实际 2480 × 3508 PNG 导出](evidence/diagram-polish/export.png)：人工查看，完整中文、主线、分支与回路清晰可见。
- [浏览器文字边界结果](evidence/diagram-polish/browser-check.json)：8 个完整中文节点全部在节点边界内，无 Vite 错误覆盖层。

回归覆盖完整中文在预览/SVG 中保留、60 字多行节点、主线居中、正交路径、菱形端点与 ELK 回退。

## 边界

未取得图一对应的原始项目 JSON，因此没有确认其单字现象是生成数据缺字还是渲染问题；不能把本次本地样例验证宣称为原始故障完全复现并闭环。已有项目的文字和坐标不会自动重写；重新布局可应用新节点尺寸与排版，若存储文字本身已缺失，需编辑文字或重新生成。未验证 Windows/Linux 字体效果。

保留工作区已有的 `docs/agent-development-spec.md` 修改及未跟踪的 `AGENTS.md`，均不纳入本次提交。
