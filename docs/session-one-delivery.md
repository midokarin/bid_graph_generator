# P0 会话一交付记录

日期：2026-09-10。范围：正式骨架、数据契约与生成后端。

## 交付范围

- 独立 React/TypeScript/Vite 前端与 FastAPI 后端，统一安装、启动、质量入口，安全忽略规则。
- Pydantic 权威的两图种业务模型、生成结果、AI 补充、项目文件、完整版本快照、布局与样式；JSON Schema 和 TypeScript 自动导出与漂移检查。
- Python 与 TS 对同一合成数据集执行结构及语义校验，包括唯一 ID、引用、甘特环、补充对象、快照一致性、数值/日期边界。
- OpenAI 兼容流式适配器、受控原生结构输出降级、两套提示词、创建任务、SSE 重连、取消、有限清理、最多两次修复、错误详情和有界临时诊断。
- 默认固定替身，环境变量配置；纯项目迁移入口与薄的 host adapter 边界。

Demo 文件未改动。会话开始时已有的 `docs/agent-development-spec.md` 人工修改及未跟踪的 `AGENTS.md` 保留原样，不纳入本次功能提交。

## 实际验证

`npm run quality` 最终通过：

| 检查 | 结果 |
|---|---|
| Pydantic → JSON Schema、Schema → TS 漂移检查 | 通过 |
| 后端 unittest | 16 项测试方法通过，含参数化场景 |
| 共享契约语料 | 63 个合成有效/无效样例，Python 与 TS 接受/拒绝结果一致 |
| 前端 Node 测试 | 64 项通过 |
| TypeScript 检查 | 通过 |
| Vite 生产构建 | 通过 |

API 测试包含两图种 SSE 成功、一次和两次修复成功、第三次无效输出后失败、甘特依赖环修复、方向权威、代码围栏、取消供应商请求、忽略不合作供应商迟到输出、重连、超时、输出上限与错误响应。OpenAI 适配器使用 `httpx.MockTransport` 测试，未调用真实供应商。

真实本机 HTTP 启动检查：默认 8000/5173 已被其他程序占用，改用 `BIAOSHU_API_PORT=8010`、`BIAOSHU_WEB_PORT=5174`，未停止原有服务。

```sh
.venv/bin/python scripts/smoke_http.py --api-port 8010 --web-port 5174
```

- 前后端启动、健康检查、前端代理、OpenAPI 响应通过。
- 替身流程图：11 个 delta、1 个 result、completed；首次 delta 到完成约 114 ms。
- 替身甘特图：11 个 delta、1 个 result、completed；首次 delta 到完成约 116 ms。
- 首个 delta 后取消：cancelled、0 个 result。
- Codex 内置浏览器首页可见“后端已连接 · 替身模型 · 契约 1.0”，无空白页/错误覆盖层，读取到的 warning/error 日志为空。该记录仅证明首页启动，未验证 Chrome/Edge 图表用户路径。
- 新增前端依赖安装审计为 0 个已知漏洞；不包含独立 Demo 依赖。
- 本次启动的服务与临时浏览器标签在验证后关闭。

沙箱最初拒绝本地端口监听，允许环境中重新启动后完成上述验证。早期测试启动器改为 Node 标准测试入口；统一质量命令现可在当前沙箱中通过。

## 边界与未完成项

会话一要求已完成。未实现会话二、三功能：SSE 工作台窗口、ELK 布局、确定性甘特排期、正式 SVG 画布、编辑/版本操作、文件读写、导出和设置页。

没有真实模型验收、Chrome/Edge 图表闭环或 Windows/Linux 启动验证。项目快照中的布局/排期示例是合成数据，不能当作排期器或渲染器证据。未执行任何模型生成代码。

技术参数集中在 `backend/app/domain/limits.py`；真实样例确认后可调整。任务与事件仅在内存保存，完成任务一小时后在下次创建时清理，上限 100 个。迁移入口只接收当前 1.0，尚无需要迁移的正式历史版本。配置页与配置持久化留给会话三。

## 下一会话前置条件

1. 阅读 `AGENTS.md`、开发规范、本交付记录，确认 Git 状态及会话一提交。
2. 安装依赖并重新运行 `npm run quality`，必须通过；保留仍未提交的人工文档修改。
3. 启动替身后端，确认所用前后端端口一致、健康检查通过。
4. 从已校验的 `result` 接入会话二；不得让中间 delta 驱动画布，不得把本次示例坐标当作自动布局结果。

本会话交付后停止，不自动进入会话二。
